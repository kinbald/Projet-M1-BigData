{% extends 'base.html.twig' %}

{% block body %}
    <h1>Universe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ universe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ universe.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ universe.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('universe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('universe_edit', { 'id': universe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
