{% extends 'baseadmin.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/create.css') }}" rel="stylesheet" />
    <link href="{{ asset('css/formulaire.css') }}" rel="stylesheet" />
    <link rel="stylesheet" href="{{ asset('uploader/fancybox/source/jquery.fancybox.css') }}">
{% endblock %}
{% block javascripts %}
    {{ parent() }}

{% endblock %}

{% block body %}
    <div id="contenu">
        <div id="content">
            <h1>{% trans %}Spirit creation{% endtrans %}</h1>
            <br><br>
            {{ form_start(form) }}
            {{ form_widget(form) }}

            <a  id="button" class="clic" href="{{ asset('uploader/fancybox.php') }}?iframe">Load a picture</a>
            <input type="text" id="chemin" name="urlImg" required>

        </div>
    </div>


    <div id="boutons">
        <div class="bouton1">
            <input class="btn btn-primary btn-circle" type="submit" value="Create" />
        </div>
        {{ form_end(form) }}

        <div class="bouton1">
            <a class="btn btn-primary btn-circle" href="{{ path('product_list', {type: 'spirit'}) }}">Back to the list</a>
        </div>

    </div>


    <div class="espace-bas"></div>
{% endblock %}
